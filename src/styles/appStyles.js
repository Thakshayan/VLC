import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  headerStyle: {
    backgroundColor: "#ffffff", // Specify the height of your custom header
    borderBottomWidth: 2,
    borderBottomColor: "#f1f1f1",
  },
  shadow: {
    shadowColor: "#7F5DF0",
    shadowOffset: {
      width: 0,
      height: 10,
    },
    shadowOpacity: 0.25,
    shadowRadius: 3.5,
    elevation: 5,
  },
});
